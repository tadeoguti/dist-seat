class LogoutUseCase {
    constructor({  }) {
      //this.AuthRepository = AuthRepository;
    }

    execute() {
      //return this.AuthRepository.saveApiKey(data);
    }
  }

  module.exports = LogoutUseCase;